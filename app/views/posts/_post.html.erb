<div id="<%= dom_id post %>" class="bg-gray-900 rounded-lg p-6 mb-4 shadow-lg">
  <p class="text-lg font-bold">
    <strong> <%= post.title %></strong>
  </p>

  <p class="mt-2 text-gray-300 leading-relaxed">
    <span class="block mt-1"><%= post.body %></span>
  </p>
</div>
