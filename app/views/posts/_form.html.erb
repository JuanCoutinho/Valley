<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div class="bg-red-600 border border-red-500 text-white p-4 rounded-lg mb-4">
      <h2 class="font-semibold"><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul class="list-disc pl-5">
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-6">
    <%= form.label :title, "Título", class: "block text-gray-300 font-semibold mb-2" %>
    <%= form.text_field :title, class: "shadow-lg appearance-none border border-gray-600 rounded-lg w-full py-3 px-4 text-gray-300 bg-gray-800 leading-tight focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-150 ease-in-out" %>
  </div>

  <div class="mb-6">
    <%= form.label :body, "Texto", class: "block text-gray-300 font-semibold mb-2" %>
    <%= form.text_area :body, class: "shadow-lg appearance-none border border-gray-600 rounded-lg w-full py-3 px-4 text-gray-300 bg-gray-800 leading-tight focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-150 ease-in-out h-32" %>
  </div>

  <div>
    <%= form.submit "Enviar", class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>
<% end %>
