<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Valley</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.2.7/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.0/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/flowbite@2.5.1/dist/flowbite.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/flowbite@2.5.1/dist/flowbite.min.js"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
   <style>
    button {
      --cyan: #00FFFF;
      font-size: 20px; /* Aumentado o tamanho da fonte */
      padding: 1em 4em; /* Aumentado o padding para o botão ser maior */
      letter-spacing: 0.1em;
      position: relative;
      font-family: inherit;
      border-radius: 0.8em; /* Aumentado o border-radius */
      overflow: hidden;
      transition: all 0.3s;
      line-height: 1.6em;
      border: 2px solid var(--cyan);
      background: linear-gradient(to right, rgba(0, 255, 255, 0.1) 1%, transparent 40%, transparent 60%, rgba(0, 255, 255, 0.1) 100%);
      color: var(--cyan);
      box-shadow: inset 0 0 10px rgba(0, 255, 255, 0.4), 0 0 9px 3px rgba(0, 255, 255, 0.1);
    }

    button:hover {
      color: #00BFFF; /* Alterado a cor do texto ao passar o mouse */
      box-shadow: inset 0 0 10px rgba(0, 255, 255, 0.6), 0 0 9px 3px rgba(0, 255, 255, 0.2);
    }

    button:before {
      content: "";
      position: absolute;
      left: -6em; /* Ajustado para se alinhar com o novo tamanho do botão */
      width: 6em;
      height: 100%;
      top: 0;
      transition: transform .4s ease-in-out;
      background: linear-gradient(to right, transparent 1%, rgba(0, 255, 255, 0.1) 40%, rgba(0, 255, 255, 0.1) 60%, transparent 100%);
    }

    button:hover:before {
      transform: translateX(20em); /* Ajustado para o novo tamanho do botão */
    }
  </style>
</head>
 <main>
    <%= yield %>  
  </main>
</html>
